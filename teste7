declare
	i, j, k, declare: int;
start
	i = 4 ;
	j = *;(((*
        tt=2;
        write(j);
        write(j)*/***/
        write("Altura é demais");
	k = i* j / k;/*
	k = 4 + a $;
	write(j);
	write(k*);
end