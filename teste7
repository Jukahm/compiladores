declare
	i, j, k, declare: int;
start
	i = 4 ;
	j = *;
        t.t=2;
        write(j);
        write(j).
        write("Altura é demais");
	k = i* j / k;/*
	k = 4 + a $;
	write(j);
	write(k*);
end
